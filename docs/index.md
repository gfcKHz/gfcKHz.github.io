# gfcKHz

Field notes for metal-sdr. Posts track validation steps, derivations, and corrections that would otherwise disappear into commit messages.

```bash
pip install -r requirements-docs.txt
mkdocs serve
```
